"use client";

// Skeleton layouts captured from the real DOM by `npm run bones` (boneyard CLI).
// The generated registry registers every skeleton and applies boneyard.config.json.
import "@/bones/registry";

export function BoneyardSetup() {
  return null;
}
